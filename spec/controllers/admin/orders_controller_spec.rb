require_relative '../../spec_helper'

describe Admin::OrdersController do
  context "generate test orders" do
    it "generates a test order and redirects to the order page" do
      pending "doesn't work yet :('"
      get admin_orders_path
    end
  end
end